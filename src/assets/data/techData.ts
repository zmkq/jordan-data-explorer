// Re-export to match the component's expected import path.
// Note: `src/data/techData.ts` casing matters for TypeScript builds.
export { techAdoptionData } from '../../data/techData'
export { techAdoptionData as default } from '../../data/techData'

